#BACKEND 

# Backend

Backend development handles the server side logic of the applications, managing the data processing, business rules, databases, authentication and others, normally exposing an API [[API - Application Programming Interface]] that a Frontend [^1]



The backend handles the process that the users doesn't see directly like storing the user data, handling processes in an scalable and secure way. 

