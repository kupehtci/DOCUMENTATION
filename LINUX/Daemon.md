#Linux 

# Daemon

A Daemon is a background process that runs indirectly from the user interaction and provides a service to the system or other programs. 

Its normally started at boot and keeps running in the background continuously executing or waiting for events or requests to handle. 

This daemons are normally managed by the [[systemd]]. 

