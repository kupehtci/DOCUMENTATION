#DOCKER 

This document is a cheat sheet that acts as a summary of common used commands for interacting with Docker Compose: 

* `docker compose up`

Run a docker compose specification. 

* `docker compose down`

Stop a docker compose specification. 

* `docker compose up --build -d`

Build and run a docker compose specification forcing a new build of the application and with process in the background. 