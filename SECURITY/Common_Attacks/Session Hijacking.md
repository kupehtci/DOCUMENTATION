#SECURITY 

# Session Hijacking

Session Hijacking os when an attacker steals or guesses an already active session ID that was created by the victim once it has logged in. 

Can be done by intercepting cookies on insecure connections and then replaying that session ID to impersonate the victim. 

