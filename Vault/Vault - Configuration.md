#VAULT 

# Vault - Configuration

[[Hashicorp Vault]] is configured using an `*.hcl` file in Hashicorp Configuration Language or HCL (Same as terraform language [[Terraform]]). 
This file defines how Vault stores the data, handles the network connections, manages the security and the integrations with external services. 

