#AWS 

# AWS - Compute Optimizer

**AWS Compute Optimizer** is a fully managed resource that analyzes EBS volumes, EC2 instances, Lambda functions and auto scaling groups and provide righ-sizing recommendations for monthly saving estimates. 

It also has the capability to optimize Fargate but only ECS on Fargate